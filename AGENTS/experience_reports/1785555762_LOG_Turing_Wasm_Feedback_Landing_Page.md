**Date:** 1785555762
**Title:** Turing WASM feedback-network landing page

## Command
python build_homepage.py followed by python -m pytest tests/test_wasm_html_shell.py tests/test_machine_targets_wasm.py -q from 	uring/.

## Log
`
8/8 backends emitted; 35 targeted tests passed.
`

## Prompt History
> another agent tried to finish up wiring a web assembly complied interest network into a turing demo website builder and module examiner, and before they could install it as the nogodsnomasters landing page they ran out of tokens, can you verify their work and finish the installation, and see about hooking those feed inputs into the nn recommendation so the demo feels alive

> and the html shell should somehow then be able to contain both mandelbrot to jpg and also inference network, technically

> the network should be transparently involved we should harvest stats not hide it

> check my last message, let's go for maximum elegance and bake into the dynamic html shell a network area by default for taking networks, handling their routing with some modification to the original mandlebrot source so we set the precedent that any module ever made comes complimentary with a neural network feedback hookup ability that just requires returning something, in this case i think we return image data right? or the location? this is the first public facing demo for my system that explains how rich and deep this goes, i want it to impress but also be the exact interface a user can use later to send requests to a server to do the compiling and provide new pages, imagine a wikipedia of algorithm in ssa, each unit comes with complimentary network hookups