# Live shader-component sentinel runtime

Allocate and validate `turing.shader-component.v1` sentinel headers in browser
and local shells, publish generation/ready/error after dispatch, let site
bundles ingest component-assembly manifests, implement concrete online endpoint
transport, and verify one mixed GLSL-compute/WGSL-compute/fragment assembly.
