# Visualization and analysis packages
matplotlib>=3.7
networkx>=3.1
scikit-learn>=1.2

# GNN controller (PyTorch Geometric requires torch and torch-scatter/cluster/etc.)
torch_geometric>=2.4

# Full beam search support (requires transformer embeddings)
# Use CPU-only PyTorch to avoid CUDA crashes
torch==1.13.1+cpu
transformers>=4.30
sentence-transformers>=2.2
